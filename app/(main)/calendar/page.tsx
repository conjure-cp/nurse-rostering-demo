import { NextPage } from "next";

const Calendar: NextPage = () => {
  return (
    <main className={"flex items-center justify-center h-full"}>
      <div className={"flex flex-col items-center justify-around h-full"}></div>
    </main>
  );
};

export default Calendar;
